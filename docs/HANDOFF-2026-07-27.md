# Handoff — 2026-07-27 session

State at handoff: **all gates green** — 1,468 tests / 126 files, typecheck, `validate:content` (61 configs), lint, and the Playwright smoke were passing at the last full run. Everything below is committed in slices on `main` (see `git log` for this date).

## What shipped today

1. **MShip01.glb** (alien interceptor from the concept board) — now on `ship.interceptor`; `LShip01.glb` moved to `ship.support`. Blender→GLB conventions: nose on Blender −Y, normalized to unit forward extent, `modelScale` = world length; engine clamps metallic ≤ 0.25 (no IBL).
2. **Render pipeline fixes** — dev MIME bug (webp served as utf8 JSON — the true reason skyboxes never showed in dev), the additive-emissive white-sky root cause (emissiveColor must stay black; intensity = `texture.level`, tint = emissive Fresnel with equal colors), skybox excluded from GlowLayer, `forceCompilationAsync` before freezing the sky material.
3. **Asteroid LOD** — distances scale with rock radius (`modelScale / LOD_REFERENCE_RADIUS`, reference 4); tier distances raised twice (high: 400/1000).
4. **Combat rework** (docs/COMBAT-REWORK.md is the spec; §13 covers beams) — manual trigger (LMB / E / Space / mobile FIRE button), `fire.mode: held | semi | continuous`, lock times 0.7/1.0/1.4, bot `fireDiscipline` on all three profiles, PROTOCOL_VERSION 2. `heatPerShot` schema-complete but **unauthored** (phase-5 balance pass pending). Beam Mk1 (`continuous`, DPS-per-tick, heat-gated ~11 s channel) is shipped content, in no default fitting.
5. **Full-loop flight** — pitch wraps (-π, π]; `maxPitchRad` is an optional legacy clamp (unauthored in shipped tuning); chase camera **rolls with the ship** (`camera.chase.pitchFollow` retired); **body-frame yaw** landed last: `turn` rotates the nose about the ship's own up (`advanceAttitude` in `shared/src/sim/math.ts`), retiring the world-yaw sign-flip entirely. Screen-steering invariant test (`client/src/game/screenSteering.test.ts`) is the acceptance gate: stick-left = nose left on screen at every pitch.
6. **HUD** — hex button cluster (family colors in `theme.hud.modules.familyColors`), one FIRE button; **boost is a toggle module** like shield (standalone button deleted; Shift = moduleToggle); module buttons show `ui.shortName` (≤12 chars); faint hollow-diamond markers for all in-view enemies (opacity floors `markerMinOpacity`), locked-target name label; FPS counter removed.
7. **World** — bubbles −30% (deep-field 210, ring-nebula 63; spawns rescaled); 90 rocks in deep-field incl. 5 colossal (radius 18); painted **planet** (deep-field gas giant, ring-nebula moon) in the skybox generator (`tools/make-skybox.mjs`, verified by `tools/verify-skybox-planets.mjs`).
8. **Tools** — Module editor tab (create/duplicate/edit/save, generic schema form, 3D stage of the module itself); module `render` block (GLB or per-family placeholder); **modules physically mount** on hulls at socket transforms (`ShipSocketRig`, shared by match view / hangar / Ship tool).

## Interrupted work (the one loose thread)

The flight agent was converting steering to body-frame yaw when it was stopped mid test-file cleanup. **The conversion itself is complete and green** (all gates passed after the stop). It reported deleting trailing describe blocks while "slicing" test files and recovering them from git; the suite passing suggests recovery finished, but the following deserve a skim next session:
- `shared/src/sim/steering.test.ts`, `shared/src/bots/flight.test.ts`, `shared/src/bots/BotIntegration.test.ts` — confirm no test cases were silently lost (compare test counts against descriptions in the files' describe blocks; `client/src/game/hud/invertedSteering.test.ts` was deliberately deleted with the flip it tested).
- `docs/BUBBLE.md` steering section — the agent was told to rewrite it for body-frame yaw; verify it matches the shipped code.

## Owner feel-checks pending (on device)
- Body-frame yaw steering at high pitch / through loops (the fix for "left/right inverted depending on up/down") — landed after the last device test.
- Planet look in-game; colossal asteroid density; smaller bubble pacing.
- Hex cluster thumb reach on phone; faint-marker visibility at range; beam laser feel.

## Open decisions for the owner
- COMBAT-REWORK.md §12: `heatPerShot` values (phase-5 balance pass), semi-auto missile confirmation (currently `semi`), content-pack compat stance (autoTarget retired outright).
- Whether the interrupted agent's test-file cleanup left anything worth re-adding (see above).

## How to continue
- Dev: `npm run dev` (client :5173) + `npm run dev:server` (:2567); desktop launcher `SpaceArenaHost.bat`.
- Full gate: `npx vitest run && npm run typecheck && npm run validate:content && npx eslint . && npm run test:e2e`.
- Skybox regen: `node tools/make-skybox.mjs content/skyboxes` (~8 min/pano), then `node tools/verify-skybox-planets.mjs`.
- The AI-agent workflow used today: Codex Sol implements from precise briefs, Opus 5 takes sim-critical/complex passes and adversarial reviews, findings loop back to Sol. Briefs live in the session scratchpad (not the repo).
