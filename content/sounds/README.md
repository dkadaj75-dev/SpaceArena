Music and other authored audio assets live in this directory and are referenced only through content/theme configuration. `MainMenu.mp3` and `Arenas.mp3` are placeholder tracks; **neither is cleared for commercial use**, so both must be replaced or properly licensed before any commercial release, with attribution or usage restrictions recorded in each track's theme `license` field.
